import React from 'react';

const VerRegistrosAli = ({ alimentosRegistrados }) => {
  return (
    <div>
      <h2>Ver Registros</h2>
      {/* Agrega el contenido específico de tu componente VerRegistros aquí */}
    </div>
  );
};

export default VerRegistrosAli;
